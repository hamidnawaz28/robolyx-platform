import React from "react";
import { makeStyles } from "@material-ui/core/styles";
const useStyle = makeStyles({
  container: {},
});
const ComplienceForm = () => {
  const { container } = useStyle();
  return <h1>Hello</h1>;
};
export default ComplienceForm;
